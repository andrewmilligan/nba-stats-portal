import { atomFamily } from 'react';

export const boxScoreAtomFamily = atomFamily({
  key: 'games.boxScoreAtomFamily',
  default: undefined,
});

export const playByPlayAtomFamily = atomFamily({
  key: 'games.playByPlayAtomFamily',
  default: undefined,
});
